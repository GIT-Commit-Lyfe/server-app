const log = require("../../utils/log");
// const controller = require('./controller')

module.exports = {
  'findById': [
    // multer.single,
    // validator,
    (req, res, next) => {
      controller
    }
  ]
}